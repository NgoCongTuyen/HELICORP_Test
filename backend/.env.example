# Chatbot hiện tại chỉ dùng dữ liệu trả lời nội bộ.
# Không cần API key hay cấu hình Gemini/OpenAI để chạy tính năng mặc định.

# Nếu bạn muốn tích hợp API thật, chỉnh `backend/index.js` trong hàm getAIResponse.
